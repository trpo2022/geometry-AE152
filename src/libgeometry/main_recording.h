#pragma once
#include "circle.h"
#include "triangle.h"

int data_recording(string* str_figures, int amount_figr, Figure* figure);
int check_input(string* str_figures, int amount_figr);
int amount_of_figures(string filename);